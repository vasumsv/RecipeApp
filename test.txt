 // Debug log the response
  logger.info("Saving invoice response: {}", responseEntity.getBody());

  // Ensure directory exists
  Path tempDir = Paths.get("target/temp/");
  if (!Files.exists(tempDir)) {
    Files.createDirectories(tempDir);
  }